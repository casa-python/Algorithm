#--------------------------------------------------------------------O
def solution(n):
    
    return int((n**(1/2)+1)**2) if n**(1/2) % 1 == 0 else -1
    # n의 제곱근이 1로 나누어 떨어지는 수(정수)면 제곱근 출력. 아니면 -1 출력.
    
#--------------------------------------------------------------------O
# 위의 컨프리헨션 구문을 풀어쓴 함수

# def solution(n):
#     if n**(1/2) % 1 == 0:
#         return int((n**(1/2)+1)**2)
#     else:
#         return -1